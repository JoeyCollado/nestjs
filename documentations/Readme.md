# npm i -g @nestjs/cli - this command install nestjs cli globally
# nest new folderName - this command creates new directory
# npm run start:dev - this command starts nestjs application
# nest g module users - this command creates new module named "users"
# nest g controller users - this command creates new controller named "users"
# nest g service users - this command creates provider for "users"

# to resolve node modules conflicts
# 1. rm -rf node_modules
# 2. pnpm install